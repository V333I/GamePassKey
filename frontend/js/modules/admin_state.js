export let allUsers = [];
export let allGames = [];
export let editingUserId = null;

export function setAllUsers(u) { allUsers = u; }
export function setAllGames(g) { allGames = g; }
export function setEditingUserId(id) { editingUserId = id; }
